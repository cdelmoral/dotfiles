---
syntax: bash
---

# Create a new session
tmux new -s <session-name>

# Attach an existing session
tmux a -t <session-name>

# Bring a separate window in the same session as right pane to current window
tmux joinp -s :<window-name> -h

# Kill session
tmux kill-session -t <session-name>

# Key bindings
d  Detach current session.
s  Select session interactively.
c  Create new window in current session.
,  Rename current window.
l  Switch to last viewed window.
;  Switch to last viewed pane.
w  Select window interactively.
%  Split the current pane into two, left and right.
!  Break the current pane out of the window.
o  Select the next pane in the current window.
